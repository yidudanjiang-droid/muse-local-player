# 安全策略

## 支持范围

当前维护分支为 `main`。请优先在最新发布版本上复现问题，并提供 Android 版本、设备型号、复现步骤和最小化日志；日志中不得包含用户文件路径、音频名称、签名口令或其他个人信息。

## 报告安全问题

请**不要**在公开 issue 中披露可被立即利用的安全问题。涉及 APK 签名、权限绕过、私有缓存泄露、路径处理、恶意媒体文件、后台服务滥用或数据读取的问题，请通过仓库维护者的私下联系方式或 GitHub 私密安全报告渠道提交。若仓库尚未启用私密报告，请先创建不含漏洞细节的公开 issue，请求安全联系方式。

报告建议包含漏洞影响、受影响版本、复现条件、概念验证、预期修复方向和你计划公开披露的时间。请给维护者合理时间确认和修复；不要上传恶意 APK、侵权音频或真实用户数据。

## 签名密钥

发布签名密钥、`keystore.properties`、口令、`.jks`/`.keystore` 文件和 CI 密钥均不属于源码。它们必须保存在受访问控制的私有位置，绝不能提交到 GitHub、issue、日志或发布包中。若怀疑密钥泄露，应立即停止使用该密钥并规划新的应用签名与发布策略。[1]

## 隐私边界

Muse 设计为离线本地播放器。安全修复不应引入未说明的遥测、广告 SDK、远程配置、媒体上传或跨应用数据收集。

## 参考资料

[1] [Android：保护应用签名密钥](https://developer.android.com/studio/publish/app-signing#secure-key)
