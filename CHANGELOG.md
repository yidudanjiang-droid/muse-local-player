# 更新日志

本项目遵循语义化版本的精神维护发布记录。

## 1.0.0 — 2026-08-25

这是 Muse 本地音乐的首个公开发布版本。

### 专题与资料库

- 提供 APK 内置专题音频包优先的首页体验，并保留设备本地媒体库作为辅助资料库。
- 支持递归发现 APK `assets/` 内的 MP3、M4A、AAC、OGG、WAV、FLAC。
- 兼容二次打包后新增的音频 assets；对被压缩的音频提供私有缓存播放回退。
- 提供 `featured_audio/pack.json` 专题文案配置、标签解析、默认元数据和稳定排序。

### 播放与可靠性

- 提供 Media3 后台播放、音频焦点、耳机拔出暂停、系统媒体通知、锁屏控制、队列、收藏、播放速度和恢复上次位置。
- 以 Media3 已就绪时间轴作为播放进度真值，避免错误文件标签导致时长和进度条跳变。
- 支持专辑详情、整专播放和从指定曲目开始播放。
- 将播放方式收敛为顺序循环、随机播放、单曲循环和不循环四种互斥策略。

### 界面与发布

- 使用 Material Design 3、动态色、手机底部导航、宽屏导航轨道和品牌开屏动画。
- 优化系统通知栏与锁屏媒体卡片：品牌小图标、明确的歌曲/艺人/专辑层级、低打扰频道和返回应用入口。
- 正式 release 变体启用 R8 代码压缩与 Android 资源收缩，移除未使用代码与资源。

### 开源与安全

- 以 MIT License 发布，并保留 MusicX（MIT）与 Audiofy（Apache-2.0）的适用 notice。
- 新增贡献、安全报告、正式发布和签名密钥管理文档。

## 1.0.1 — 2026-08-25

本次为首发后的体验与发行修复版本。

| 范围 | 更新 |
|---|---|
| 首次授权 | 开屏结束后会主动请求缺失的设备音频读取与 Android 13+ 通知权限；首页持续显示两项权限状态，并分别提供再次授权入口。 |
| 授权恢复 | 用户永久拒绝后，授权按钮会转到应用详情设置；从设置返回后界面和资料库状态会立即刷新。 |
| 混音播放 | 播放页新增“混音播放”开关。开启后 Muse 不再请求音频焦点，可与其他应用声音同时输出；关闭时恢复标准独占协调。 |
| 淡入淡出 | 播放页新增“淡化”开关；应用内切歌、上一首、下一首、暂停和恢复播放使用约 280ms 音量过渡。 |
| 共存安装 | 媒体通知频道改为以运行时包名派生；使用改包名工具共存安装后，通知、权限、DataStore、缓存和后台媒体服务独立隔离。 |
| ABI 资产 | 正式 Release 提供 `arm64-v8a`、`armeabi-v7a`、`x86_64`、`x86` 四个专用 APK 与一个 universal APK。 |

## 1.0.2 — 2026-08-25

本次为播放稳定性与大媒体库体验打磨版本。

| 范围 | 更新 |
|---|---|
| 平滑切歌 | 从歌曲列表、专辑、队列切换曲目时，会复用淡入淡出策略；不再只有播放器页上一首/下一首具备过渡效果。 |
| 生命周期 | 页面控制器释放前恢复正常音量，避免极端情况下淡化任务中断后后台播放器延续低音量状态。 |
| 启动性能 | 删除已授权启动路径的重复媒体库扫描，降低大设备媒体库打开首页时的无效 I/O。 |
| 专题缓存 | 内置/二次打包 assets 使用 UUID 派生的稳定曲目标识和缓存文件名，降低路径哈希碰撞风险；启动时会清理遗留的 `.part` 缓存。 |
| 封面性能 | 曲目和队列封面统一限制为 384px 解码，使用稳定内存与磁盘缓存键，减少高分辨率封面在快速滚动时的解码与内存压力。 |
| 发行 | 继续提供 `arm64-v8a`、`armeabi-v7a`、`x86_64`、`x86` 以及 universal 五种已签名 APK。 |

## 1.1.0 — 2026-08-26

这是一次性加入日常高频能力的功能包版本。

| 范围 | 更新 |
|---|---|
| 睡眠定时 | 首页新增 15、30、60 分钟定时；结束时按淡化设置平滑暂停。结束时间持久化并在应用返回后恢复。 |
| 播放历史 | 记录最近 50 首实际切入播放的曲目，自动去重；首页可直接继续播放或一键清空。 |
| 最近加入 | 首页展示最近加入的 8 首设备音乐；MediaStore 变化后自动刷新。 |
| 批量播放 | 收藏页新增“播放全部收藏”，一键建立连续播放队列。 |
| 私隐与隔离 | 历史、定时和最近加入均存于应用私有空间，不上传网络，并与共存 APK 按包名隔离。 |
| 发布 | 版本升至 `1.1.0`（`versionCode=4`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.1.1 — 2026-08-26

这是连续机制包版本，集中完成播放和队列的高频效率改进。

| 范围 | 更新 |
|---|---|
| 队列撤销 | 清空队列后可在提示中撤销，恢复清空前队列及选中曲目。 |
| 批量追加 | 专辑详情和收藏页可将整组曲目追加到当前队列，自动跳过重复歌曲并反馈实际新增数量。 |
| 迷你播放器 | 增加直达播放队列按钮，缩短队列管理路径。 |
| 定时控制 | 播放页同步展示当前睡眠定时状态，并可直接调整 15、30、60 分钟或关闭。 |
| 发行 | 版本升至 `1.1.1`（`versionCode=5`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.1.2 — 2026-08-26

本版本继续聚焦播放可靠性与高频操作的连续性。

| 范围 | 更新 |
|---|---|
| 前台性能 | 从后台或系统设置返回时，仅在设备音频权限实际发生变化时刷新资料库，避免已授权用户被重复扫描。 |
| 队列撤销 | 单曲移出队列后可立即撤销，并恢复到移除前的位置；清空队列撤销机制继续保留。 |
| 队列恢复 | 当队列已在内存中而媒体控制器尚未载入项目时，点按队列歌曲会正确装载并播放。 |
| 播放恢复 | 接近曲目结尾时不再持久化“结尾位置”，避免重启或恢复后误触发立即结束。 |
| 异常文件 | 当已扫描的本地文件被删除、移动或无法解码时，会自动跳过并继续下一首，避免连续播放直接中断。 |
| 反馈一致性 | 大屏/宽屏布局与手机布局均会显示批量加入队列的实际结果。 |
| 发行 | 版本升至 `1.1.2`（`versionCode=6`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.1.3 — 2026-08-26

这是启动器图标兼容修复版本。

| 范围 | 更新 |
|---|---|
| 自适应图标 | 新增 Android 自适应图标资源，使用深蓝全出血背景与独立前景标记，适配圆形、圆角方形等系统启动器遮罩。 |
| 白边修复 | 替换旧版带白色方底的圆角位图；启动器图标四角不再出现白色留边。 |
| 品牌一致性 | 抽屉与关于页使用专用品牌展示资源，避免 Compose 直接加载自适应启动图标而产生兼容问题。 |
| 发行 | 版本升至 `1.1.3`（`versionCode=7`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.1.4 — 2026-08-26

本版本借鉴 Apache-2.0 开源播放器的队列可读性实践，并按 Muse 的 Material 3 架构重新实现。

| 范围 | 更新 |
|---|---|
| 队列分段 | 播放队列会清晰划分“队列前序”“正在播放”“接下来”，长队列中能立即判断当前播放位置与后续内容。 |
| 自动定位 | 打开播放队列时会自动滚动到当前曲目区域，不必手动在长队列中查找。 |
| 当前状态 | 队列标题显示总曲数及当前播放序号；当前曲目继续使用 Material 3 容器色强调。 |
| 开源实践 | 仅参考 Audiofy、UAMP 与 Compose 音乐播放器样例的 Apache-2.0 机制思路；未复制其源代码。具体记录见 `docs/open_source_inspiration_1.1.4.md`。 |
| 发行 | 版本升至 `1.1.4`（`versionCode=8`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.1.5 — 2026-08-26

本版本参考商业播放器公开帮助文档中的通用队列整理模式，并以原创方式适配至离线本地音乐场景。

| 范围 | 更新 |
|---|---|
| 移除已播 | 当前曲目前出现“移除已播”入口，可一键清理已经播放完的队列前序，不影响当前曲目和接下来播放顺序。 |
| 即时撤销 | 整理后可在 Snackbar 中撤销，完整恢复被移除的已播曲目及原队列顺序。 |
| 状态保护 | 当队列没有已播前序时不显示整理操作，避免无效点击；其他移动、单曲移除和清空操作会使旧整理撤销失效，防止状态冲突。 |
| 商业体验边界 | 仅参考 Spotify 与 Apple Music 官方公开帮助文档中“查看 / 整理 / 清空队列”的通用体验原则；未复制品牌、代码、视觉资产或服务功能。具体记录见 `docs/commercial_product_inspiration_1.1.5.md`。 |
| 发行 | 版本升至 `1.1.5`（`versionCode=9`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.1.6 — 2026-08-26

本版本强化 APK 内专题音频优先的核心播放路径。

| 范围 | 更新 |
|---|---|
| 完整专题播放 | 首页专题主卡片与“全部播放”现在会以专题仓库的原始顺序建立完整连续队列，而非在已有队列中只追加第一首。 |
| 队列隔离 | 用户从设备音乐或收藏切换到专题播放时，会明确切换为当前专题音频队列，避免旧队列残留干扰专题连续收听。 |
| 资源顺序 | 专题播放直接复用 assets 扫描后的稳定排序，不对打包专题的文件顺序进行二次专辑排序。 |
| 宽屏一致性 | 手机与宽屏导航布局使用同一完整专题播放回调。 |
| 发行 | 版本升至 `1.1.6`（`versionCode=10`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.1.7 — 2026-08-26

这是以正式 APK 组成分析为依据的剪枝优化版本。

| 范围 | 更新 |
|---|---|
| 视觉资源 | 开屏背景从 1440×2560 PNG 精简为 1080×1920 高质量 WebP；专题封面从 1536×1024 PNG 精简为 1152×768 高质量 WebP，保留原始构图与显示安全区域。 |
| 依赖剪枝 | 删除经全源码检索确认未使用的 `navigation-compose` 依赖。 |
| 包体积 | universal APK 从 7,925,961 B 降至 2,991,003 B，减少 4,934,958 B（62.26%）；arm64-v8a 从 7,828,025 B 降至 2,893,067 B，减少 4,934,958 B（63.04%）。 |
| 保留能力 | APK 内专题音频和 `noCompress` 音频策略、Media3 后台播放、通知、DataStore、Coil 封面缓存、四 ABI + universal 输出与共存隔离均未删除。 |
| 发行 | 版本升至 `1.1.7`（`versionCode=11`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.1.8 — 2026-08-26

本版本强化 APK 专题音频在二次打包和长期维护场景中的确定性与容错能力。

| 范围 | 更新 |
|---|---|
| 曲目清单覆盖 | `featured_audio/pack.json` 可选新增 `tracks` 对象；发布者可按 assets 路径精确覆盖标题、艺人、专辑、曲序和年份，不再依赖标签质量或文件名规则。 |
| 路径兼容 | 覆盖键支持完整 `featured_audio/` 路径、省略此前缀的相对路径以及反斜杠路径标准化。 |
| 稳定排序 | 同曲序、同标题的音频会进一步按 assets 路径排序，避免二次打包工具改变目录遍历顺序导致展示或播放顺序漂移。 |
| 异常隔离 | 单个 assets 音频损坏、标签解析异常或压缩缓存准备失败时会被跳过；其余专题曲目仍可正常显示和播放。 |
| 发行 | 版本升至 `1.1.8`（`versionCode=12`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.1.9 — 2026-08-26

本版本让每个专题 APK 可通过自身 `pack.json` 维护独立视觉身份。

| 范围 | 更新 |
|---|---|
| 专题封面 | `pack.json` 顶层新增可选 `coverAsset`；可指向 assets 内 `.png`、`.webp`、`.jpg` 或 `.jpeg`，解析后同步用于首页专题主卡片和专题曲目封面。 |
| 路径兼容 | 支持 `featured_audio/cover.webp`、`cover.webp` 和反斜杠路径；路径标准化后自动检查 assets 是否存在。 |
| 安全回退 | 未配置、路径错误、文件缺失或格式不支持时自动使用内置默认封面，不影响专题扫描与播放。 |
| 解码控制 | 首页封面继续使用受限尺寸、内存/磁盘缓存键与关闭 crossfade 的 Coil 请求，避免高分辨率封面带来额外热点压力。 |
| 发行 | 版本升至 `1.1.9`（`versionCode=13`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.1.10 — 2026-08-26

本版本强化设备媒体库变化后的队列维护与持续收听体验。

| 范围 | 更新 |
|---|---|
| 队列重校验 | MediaStore 自动刷新或手动重扫完成后，当前播放队列会根据最新媒体索引重新匹配；已删除、移动或不再可读的设备文件会从队列中移除。 |
| 连续播放保护 | 队列存在有效曲目时，Media3 以当前曲目优先、否则以安全下标重建队列；原本正在播放的任务会继续播放。 |
| 空队列保护 | 若刷新后整个队列均不可用，播放器媒体项、当前曲目和时间轴会被明确清空，不会遗留失效的恢复状态。 |
| 恢复同步 | 清理后的队列会写回 DataStore，后续启动不再反复尝试恢复同一批已失效文件。 |
| 状态反馈 | 应用会说明媒体库更新后移除了多少首不可用音频，或明确提示整个队列已失效。 |
| 发行 | 版本升至 `1.1.10`（`versionCode=14`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.1.11 — 2026-08-26

本版本缩短了专题音频的高频收听路径。

| 范围 | 更新 |
|---|---|
| 专题快捷入队 | 首页专题主卡片新增“加入队列”，可一次加入当前专题全部曲目，而无需逐首打开更多菜单。 |
| 不打断收听 | “从头播放”仍会明确建立完整专题播放队列；“加入队列”则保留当前正在播放的内容，仅将专题的新增曲目追加到后续计划。 |
| 去重与反馈 | 复用既有批量入队去重，已存在曲目不会重复追加；手机和宽屏布局均会显示实际新增数量或“已在队列”反馈。 |
| 发行 | 版本升至 `1.1.11`（`versionCode=15`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.1.12 — 2026-08-26

本版本让长队列的后续收听计划在打开队列时直接可见。

| 范围 | 更新 |
|---|---|
| 后续时长摘要 | 队列标题现显示当前播放序号与“接下来”曲目的累计可听时长，用户无需手动逐首计算剩余计划。 |
| 未知时长诚实呈现 | 仍未由 Media3 解码确认时长的曲目会被单独计数；若全部后续时长尚未解析，界面显示“正在读取时长”而不是误报 `0:00`。 |
| 状态联动 | 时长在播放过程中被 Media3 回写后，队列摘要会随 Compose 状态更新；与现有真实时间轴原则保持一致。 |
| 发行 | 版本升至 `1.1.12`（`versionCode=16`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.3.0 — 2026-08-26

这是一次以 APK 内专题内容可维护性为核心的播放体验升级：歌词由发布者随专题音频离线提供，并直接进入动态播放舞台。

| 范围 | 更新 |
|---|---|
| LRC 侧车发现 | 专题音频可在相同目录放入同名 UTF-8 `.lrc` 文件，例如 `01-序章.mp3` 对应 `01-序章.lrc`；二次打包 assets 同样适用。 |
| 时间轴解析 | 支持一个或多个 `[分:秒.毫秒]` / `[分:秒:毫秒]` 标签；空白或无效行会安全忽略，不影响播放。 |
| 动态歌词舞台 | 播放页检测到有效歌词后自动压缩封面区域，在既有动态声场中展示前一行、当前高亮行与后一行。 |
| 播放同步 | 歌词状态随当前 Media3 曲目异步加载，换歌、清空队列、媒体库失效和恢复上次播放时都会取消或切换到正确的本地歌词。 |
| 隐私边界 | 不联网搜索、下载、生成或猜测歌词；未提供 LRC 时保持原有无歌词播放界面。 |
| 发行 | 版本升至 `1.3.0`（`versionCode=21`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.2.1 — 2026-08-26

本版本让播放页从静态控制面板升级为具有呼吸感的原创声场舞台。

| 范围 | 更新 |
|---|---|
| 播放中声场 | 播放页底部弹层接入原创 AI 声场原画，以云雾粒子、光带和低饱和蓝紫色形成深层背景。 |
| 端侧动态 | 播放时使用低频的背景漂移与呼吸光晕，原画本身不播放视频、不请求网络、不增加解码器负担。 |
| 暂停节制 | 暂停后声场固定在静态构图并降低亮度，不再营造持续运动感，也避免不必要的视觉干扰。 |
| 前景可读性 | 动态原画之上保留半透明阅读层，曲名、进度、播放策略、声音处理与睡眠定时在深色背景中保持清晰。 |
| 歌词预留 | 当前未伪造歌词内容；未来接入本地 LRC 等歌词来源后，可复用相同声场舞台承载逐行歌词。 |
| 发行 | 版本升至 `1.2.1`（`versionCode=20`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.2.0 — 2026-08-26

这是一次以原创品牌素材为核心的视觉升级版本，强化每个专题 APK 作为独立内容载体的第一印象与辨识度。

| 范围 | 更新 |
|---|---|
| 专题主视觉 | 首页专题 Hero 的默认回退图升级为原创 AI 声场原画：深钴蓝、云雾粒子与克制的声波丝带形成可叠加内容文字的沉浸式舞台。 |
| 默认专辑封面 | 无内嵌/外部封面的设备音频不再只是纯色音乐符号，改用 Muse 专属的“声场唱片”原画，并保留轻量图标识别。 |
| 开屏氛围 | “白云科技 / 6G 电信”开屏更换为原创云端声场视觉，保留原有快速淡入与文字可读性，并加深层次遮罩。 |
| 专题可维护性 | `pack.json` 的 `coverAsset` 仍拥有最高优先级；发布者放入自己的专题封面后，会自动覆盖默认品牌主视觉。 |
| 体积控制 | 三项新增资源均以受控尺寸 WebP 纳入 `drawable-nodpi`，不引入网络加载或额外运行时依赖。 |
| 发行 | 版本升至 `1.2.0`（`versionCode=19`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.1.14 — 2026-08-26

本版本让异常音频从自动容错走向可控恢复，减少用户在坏文件与长队列间来回操作的成本。

| 范围 | 更新 |
|---|---|
| 一键重试 | 自动跳过无法播放的音频时，界面提示会直接提供“重试”操作；无需离开当前页面或手动在长队列中查找该文件。 |
| 失败定位 | 提示会显示失败曲目的标题，并保留最近一次失败文件作为安全重试目标。 |
| 错误状态恢复 | 单曲播放或队列候选耗尽时，重试会明确重新准备当前 Media3 媒体项，再恢复播放；不再只尝试对错误状态调用播放。 |
| 保护边界 | 重试只解除目标文件的失败标记；其他已确认失败文件仍会在本轮连续播放中被跳过。 |
| 发行 | 版本升至 `1.1.14`（`versionCode=18`），继续提供四 ABI 专用 APK 和 universal APK。 |

## 1.1.13 — 2026-08-26

本版本加强循环、随机和长队列中的异常音频容错。

| 范围 | 更新 |
|---|---|
| 连续失败隔离 | 当前文件播放失败后，Muse 会按 Media3 当前顺序、随机与循环规则继续寻找下一个未失败曲目，不会因成功切换到下一首就遗忘本轮坏文件。 |
| 防重复卡顿 | 在随机或循环策略下，已经确认失败的文件会被连续跳过，避免不断回到同一坏文件造成停顿。 |
| 用户重试 | 用户主动点按歌曲或队列项目时，会移除该曲目的失败标记，允许在文件修复、设备状态变化后手动重试。 |
| 新播放上下文 | 显式播放新的专辑、专题或列表时会清空旧失败集合，使每一套新队列都有一次完整尝试机会。 |
| 发行 | 版本升至 `1.1.13`（`versionCode=17`），继续提供四 ABI 专用 APK 和 universal APK。 |
